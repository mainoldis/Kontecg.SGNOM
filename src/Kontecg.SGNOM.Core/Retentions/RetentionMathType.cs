namespace Kontecg.Retentions
{
    /// <summary>
    /// Specifies the type of calculation to use in the formula
    /// </summary>
    public enum RetentionMathType
    {
        Percent = 1,
        Fixed = 2,
        RatePerWorkDay = 3,
    }
}