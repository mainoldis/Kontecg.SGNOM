namespace Kontecg.Workflows
{
    /// <summary>
    /// ForReview, Reviewed and Confirmed for all document that needs review and confirmation
    /// </summary>
    public enum ReviewStatus
    {
        ForReview = 0,
        Reviewed = 1,
        Confirmed = 2,
        Canceled = 3
    }
}
